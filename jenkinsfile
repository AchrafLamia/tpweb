// Jenkinsfile

pipeline {
    agent any

    stages {
        stage('Cloning Git') {
            steps {
                git 'https://github.com/AchrafLamia/tpweb'
            }
        }

      stage('Deploy image') {
steps{
bat "docker run -d $registry:$BUILD_NUMBER"
}
}

        stage('Test image') {
            steps {
                script {
                    // Your test commands go here
                    echo 'Testing the Docker image...'
                }
            }
        }

        stage('Publish Image') {
            steps {
                script {
                    docker.withRegistry('https://registry.hub.docker.com', 'docker') {
                        def customImage = docker.build('achraf60/dockervf')
                        customImage.push()
                    }
                }
            }
        }

        stage('Deploy Image') {
            steps {
                script {
                    // Your deployment commands go here
                    echo 'Deploying the Docker image to Docker Engine...'
                    docker.withServer('tcp://localhost:2376', 'docker') {
                        docker.image('achraf60/dockervf').push()
                    }
                }
            }
        }
    }

    post {
        always {
            echo 'Pipeline completed!'
        }
    }
}
